# Copy this file to ".env" and fill in your Supabase project values.
# Find them in your Supabase dashboard: Project Settings → API.
#
# If you leave these blank, the app runs in DEMO MODE with built-in sample
# data (read-only) so you can see everything working immediately.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
